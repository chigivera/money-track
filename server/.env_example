# Database Connection URI
MONGODB_URI=mongodb+srv://username:password@cluster0.mongodb.net/myFirstDatabase?retryWrites=true&w=majority

# Application Port
APP_PORT=8000

# Secret Key for JWT Tokens (if using JSON Web Tokens for authentication)
JWT_SECRET=mysecretpassword123

# CORS Configuration (optional, depending on your CORS setup)
CORS_ORIGIN=http://localhost:3000

# SMTP Credentials
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@example.com
SMTP_PASS=yourpassword

# Email Sending Options
EMAIL_FROM="Your Company Name"
EMAIL_TO="recipient@example.com"
EMAIL_SUBJECT="Welcome to Our Service"
EMAIL_BODY="Thank you for subscribing!"